export interface MainFeature {
  id: number;
  title: string;
  description: string;
  icon: string;
  video: string;
}

export interface SubFeature {
  id: number;
  title: string;
  description: string;
  icon: string;
}
